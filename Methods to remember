def convert(num_str, n, m): #n進数をm進数に変換する
    decimal = int(num_str, n) #10進数に変換
    if decimal == 0:
        return 0
    else:
        digits = []
        while decimal > 0:
            digits.append(str(decimal % m))
            decimal //= m
        return "".join(reversed(digits)) #n進数を文字列で返す
